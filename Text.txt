값 타입 vs 참조타입
-스택영역에 값을 저장 참조타입은 값을 힙(메모리)영역에 저장하고 주소를 스택영역에 저장
문자열은 값타인인가 참조타입인가
-문자열은 참조타입(float int 등은 값타입 vector3 string등은 참조타입)
static은 무슨 키워드
-static은 정적 키워드
c#에서 객체타입 (object 타입)설명, 객체타입의 장점과 단점
c#에서 객체타입의 단점을 커버하기 위해서 어떤 방식을 권유하고 있는지?(또는 어떻게 해결하는지)
string의 연산시 일어나는 과정 설명
string의 연산을 했을 때 발생하는 문제점을 해경하는 방법(아직 수업 X)
foreach문의 원리 설명
IEnumerator와 IEnumerable설명
IDisposable은 뭐하는 인터페이스인지
using 키워드의 사용방법들은 어떤 것이 있는지
동적 배열의 탐색시간은 빅오노테이션으로?
동적 배열의 삽입시간으느 빅오노테이션으로?
해시테이블의 탐색시간은 ?
해시테이블에서 key의 타입이 문자열이라고할 때 어떤 식으로 해시함수를 만들 수 있을지
이진트리구조의 장점
merge sort는 빅오노테이션으로 시간복잡도 O(NLogN) QuickSort는 빅오노테이션으로 시간복잡도 O(N^2)인데 실제로 어떤 것이 더 빠르고 왜 빠른지
c#의 인터페이스가 가질 수 있는 멤버의 종류
LinkedList는 어떤 상황에서 사용을 하면 좋은가
캐시메모리의 역할
c#의 structure와 class의 차이는
c#에서 그럼 어떤 경우에 class 대신 strictire를 쓰는 것이 좋을지
GC(Garbage collection)설명
GC가 세대를 구분하는 이유